diff --git a/make-linux-appimage-nomidi.sh b/make-linux-appimage-nomidi.sh
index f43a1a4..e5001c5 100644
--- a/make-linux-appimage-nomidi.sh
+++ b/make-linux-appimage-nomidi.sh
@@ -1,5 +1,8 @@
 #!/bin/bash
 
+# Regenerate embedded Fast Tracks artwork from the editable BMP.
+python3 scripts/update_fasttracks_logo.py || exit 1
+
 LINUXDEPLOY="linuxdeploy-$(uname -m).AppImage"
 BUILDDIR="release/linux"
 
diff --git a/make-linux-appimage.sh b/make-linux-appimage.sh
index ec9a144..93c893c 100644
--- a/make-linux-appimage.sh
+++ b/make-linux-appimage.sh
@@ -1,5 +1,8 @@
 #!/bin/bash
 
+# Regenerate embedded Fast Tracks artwork from the editable BMP.
+python3 scripts/update_fasttracks_logo.py || exit 1
+
 LINUXDEPLOY="linuxdeploy-$(uname -m).AppImage"
 BUILDDIR="release/linux"
 
diff --git a/make-linux-nomidi.sh b/make-linux-nomidi.sh
index 45f99aa..7dc3bdf 100644
--- a/make-linux-nomidi.sh
+++ b/make-linux-nomidi.sh
@@ -1,5 +1,8 @@
 #!/bin/bash
 
+# Regenerate embedded Fast Tracks artwork from the editable BMP.
+python3 scripts/update_fasttracks_logo.py || exit 1
+
 rm release/other/ft2-clone &> /dev/null
 echo Compiling \(with no MIDI functionality\), please wait patiently...
 
diff --git a/make-linux.sh b/make-linux.sh
index 509c028..ae819b4 100755
--- a/make-linux.sh
+++ b/make-linux.sh
@@ -1,5 +1,8 @@
 #!/bin/bash
 
+# Regenerate embedded Fast Tracks artwork from the editable BMP.
+python3 scripts/update_fasttracks_logo.py || exit 1
+
 rm release/other/ft2-clone &> /dev/null
 echo Compiling, please wait patiently...
 
diff --git a/make-macos.sh b/make-macos.sh
index 5e9d646..259bd2a 100755
--- a/make-macos.sh
+++ b/make-macos.sh
@@ -31,6 +31,11 @@ if [ ! -d $TARGET_DIR ]; then
     mkdir -p $TARGET_DIR
 fi
 
+#
+# Regenerate embedded Fast Tracks artwork from the editable BMP.
+#
+python3 scripts/update_fasttracks_logo.py || exit 1
+
 #
 # Compile
 #
diff --git a/vs2026_project/ft2-clone/ft2-clone.vcxproj b/vs2026_project/ft2-clone/ft2-clone.vcxproj
index bfb2fd5..0464071 100644
--- a/vs2026_project/ft2-clone/ft2-clone.vcxproj
+++ b/vs2026_project/ft2-clone/ft2-clone.vcxproj
@@ -129,6 +129,9 @@
       <FixedBaseAddress>false</FixedBaseAddress>
       <AdditionalManifestDependencies>"type='win32' name='Microsoft.Windows.Common-Controls' version='6.0.0.0' processorArchitecture='*' publicKeyToken='6595b64144ccf1df' language='*'";%(AdditionalManifestDependencies)</AdditionalManifestDependencies>
     </Link>
+    <PreBuildEvent>
+      <Command>py -3 &quot;$(ProjectDir)..\..\scripts\update_fasttracks_logo.py&quot;</Command>
+    </PreBuildEvent>
     <PostBuildEvent />
     <Manifest />
     <Manifest>
@@ -184,6 +187,9 @@
       <AdditionalDependencies>SDL2main.lib;SDL2.lib;shlwapi.lib;winmm.lib;%(AdditionalDependencies)</AdditionalDependencies>
       <AdditionalManifestDependencies>"type='win32' name='Microsoft.Windows.Common-Controls' version='6.0.0.0' processorArchitecture='*' publicKeyToken='6595b64144ccf1df' language='*'";%(AdditionalManifestDependencies)</AdditionalManifestDependencies>
     </Link>
+    <PreBuildEvent>
+      <Command>py -3 &quot;$(ProjectDir)..\..\scripts\update_fasttracks_logo.py&quot;</Command>
+    </PreBuildEvent>
     <PostBuildEvent>
       <Message>
       </Message>
@@ -236,8 +242,7 @@
       </Command>
     </PostBuildEvent>
     <PreBuildEvent>
-      <Command>
-      </Command>
+      <Command>py -3 &quot;$(ProjectDir)..\..\scripts\update_fasttracks_logo.py&quot;</Command>
     </PreBuildEvent>
     <Manifest>
       <EnableDpiAwareness>PerMonitorHighDPIAware</EnableDpiAwareness>
@@ -284,8 +289,7 @@
       </Command>
     </PostBuildEvent>
     <PreBuildEvent>
-      <Command>
-      </Command>
+      <Command>py -3 &quot;$(ProjectDir)..\..\scripts\update_fasttracks_logo.py&quot;</Command>
     </PreBuildEvent>
     <ResourceCompile>
       <PreprocessorDefinitions>_WIN64</PreprocessorDefinitions>
--- a/scripts/update_fasttracks_logo.py
+++ b/scripts/update_fasttracks_logo.py
@@ -54,6 +54,31 @@
     return palette
 
 
+
+def decode_uncompressed_4bit(
+    data: bytes, pixel_offset: int, width: int, height: int
+) -> list[list[int]]:
+    row_stride = ((width * 4 + 31) // 32) * 4
+    rows = []
+    top_down = height < 0
+    abs_height = abs(height)
+
+    for out_y in range(abs_height):
+        src_y = out_y if top_down else abs_height - 1 - out_y
+        start = pixel_offset + src_y * row_stride
+        end = start + ((width + 1) // 2)
+        if end > len(data):
+            raise ValueError("truncated 4-bit BMP pixel data")
+
+        row = []
+        for packed in data[start:end]:
+            row.append((packed >> 4) & 0x0F)
+            if len(row) < width:
+                row.append(packed & 0x0F)
+        rows.append(row)
+
+    return rows
+
 def decode_uncompressed_8bit(
     data: bytes, pixel_offset: int, width: int, height: int
 ) -> list[list[int]]:
@@ -123,13 +148,16 @@
     pixel_offset, dib_size, width, height, bpp, compression = bmp_info(data, path)
     palette = read_palette(data, dib_size, bpp)
 
-    if bpp == 8 and compression == 0:
+    if bpp == 4 and compression == 0:
+        indices = decode_uncompressed_4bit(data, pixel_offset, width, height)
+    elif bpp == 8 and compression == 0:
         indices = decode_uncompressed_8bit(data, pixel_offset, width, height)
     elif bpp == 8 and compression == 1:
         indices = decode_rle8(data, pixel_offset, width, height)
     else:
         raise ValueError(
-            "editable BMP must be 8-bit indexed (uncompressed or BI_RLE8); "
+            "editable BMP must be 4-bit or 8-bit indexed "
+            "(uncompressed, or BI_RLE8 for 8-bit); "
             f"got {bpp}-bit compression type {compression}"
         )
 
